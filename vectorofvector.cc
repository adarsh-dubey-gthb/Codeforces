#include<bits/stdc++.h>
using namespace std;
int main(){
    vector<vector<int>>vv(10,vector<int>(5,100));
    for (int i = 0; i < vv.size(); i++)
    {
        for (int i = 0; i < vv.size(); i++)
        {
            for (int j = 0; i < vv[i].size(); i++)
            {
                cout<<vv[i][j]<<" ";
            cout<<endl;
            }
            
        }
        
    }
    
}